---
schema_version: 1
id: AL-20260914-016
kind: agent_log
title: 対象アプリ識別（for ○○）の実装とテスト
task_type: implementation
status: done
entrypoint: path-a
trigger: REQ-0030 / SPEC-0025 / DSN-0020 — SubjectContext のランタイム実装
related: [NEED-0010, REQ-0030, SPEC-0025, DSN-0020, TC-0057, TC-0058, TC-0059, TC-0060]
created: "2026-09-14"
updated: "2026-09-14"
---

## Content

### Objective

**課題:** V字文書で定義した SubjectContext がランタイムに未実装のため、Web・CLI・MCP で対象アプリ名が表示されない。

**対応:** 解決器を追加し `TraceWeaveReport.subject` を正本として全表面へ伝播。TC と自動テストで REQ-0030 の AC を検証する。

### Completion Criteria

- [x] `resolveSubjectContext` と package 名共有ヘルパー実装
- [x] Web ヘッダー・document.title・レポーター・serve・MCP 反映
- [x] TC-0057〜0060 起票とテスト追加
- [x] lint / test / traceweave check 成功

### Changes

**解決とレポート配線**  
`resolve-subject-context.ts` で CLI・環境変数・`.traceweave/config.json`・パッケージ名・ディレクトリ名の優先順を実装。`buildTraceWeaveReport` が `subject` を付与し adopt probe と package 名解決を共有する。

**UI・CLI・MCP 表面**  
Header に `for {displayName}`、App でタイトル更新、Markdown/Console 先頭行、グローバル `--subject`、`serve` 起動案内、MCP 要約の `{ subject, summary }` を実装した。

**テスト**  
解決優先順・レポート同梱・表示フォーマット・MCP 返却のテストを追加。serve 起動メッセージ変更に合わせ CLI 結合テストの待機条件を `is running at` に更新した。

### Outcome

ダッシュボード・ターミナル・エクスポート・MCP のいずれでも、品質保証対象アプリ名を一貫して識別できる。設定ファイルまたは `--subject` で上書き可能。

### Improvements

N/A

### Verification

```bash
npm --prefix src run lint          # exit 0
npm --prefix src test              # 137 pass, 0 fail
./bin/traceweave check             # exit 0
```

### Notes

文書起票は AL-20260914-015。モノレポ複数 subject は将来 NEED。
