---
schema_version: 1
id: AL-20260914-007
kind: agent_log
title: 設計思想・仕様整合性改善（4フェーズ）
task_type: improvement
status: done
entrypoint: path-d
trigger: 設計思想と仕様の食い違い走査後、改善フェーズ1→4の実施指示
related: [SPEC-0004, SPEC-0005, SPEC-0023, REQ-0029, DSN-0019, QA-0001, QA-0002]
created: "2026-09-14"
updated: "2026-09-14"
---

## Content

### Objective

**課題:** 走査で見つかった設計思想・仕様・実装の乖離（MCP 未文書化、SPEC-0005 の旧スキーマ、URL `q` 未連携、CLI エラー不統一、QA Evidence の ADR-0006 違反、テスト地層の偏り等）が、利用者契約と V モデル追跡の信頼性を損ねていた。

**対応:** 文書追従（フェーズ1）→ 境界明確化（フェーズ2）→ 契約バグ修正（フェーズ3）→ ガバナンス整備（フェーズ4）の順で、仕様・実装・検証を一括整合させる。

### Completion Criteria

- [x] SPEC-0004/0005 と QA Evidence を実装・ADR-0006 に整合
- [x] MCP の REQ/SPEC/DSN 起票と SYSTEM_OVERVIEW 境界の明確化
- [x] URL `q` 横断検索、CLI エラーハンドリング、`/api/test/run` 404/400、catalog `--status`
- [x] superseded 文書整理、新規 TC（0039–0041）、validate-docs の superseded 扱い
- [x] `npm --prefix src run lint` および `npm --prefix src test` が成功

### Changes

ファイル一覧・差分:

```bash
git show "$(git log --grep 'AL-20260914-007' -1 --format=%H)" --stat
git switch --detach "$(git log --grep 'AL-20260914-007' -1 --format=%H)" && git switch -
```

| Area | Summary |
|------|---------|
| 文書是正（フェーズ1） | **状況:** SPEC-0004/0005 と QA Evidence が実装・ADR-0006 と乖離していた。**対応:** SPEC-0004 に全 CLI とグローバルオプションを追記、SPEC-0005 を現行 `TraceWeaveReport` と 5 タブへ改訂。QA Evidence を `reports/test-results.json` 参照に切替。**狙い:** 文書が実装契約を正しく記述する。 |
| 境界明確化（フェーズ2） | **状況:** MCP が V モデル未接続、adopt の境界が曖昧だった。**対応:** REQ-0029・SPEC-0023・DSN-0019 を起票し、SYSTEM_OVERVIEW で MCP をオプショナル拡張・adopt を Out-of-Scope と明示。**狙い:** システム境界が正本で追跡できる。 |
| 契約バグ修正（フェーズ3） | **状況:** URL `q` 未連携、CLI エラー不統一、`/api/test/run` 分岐不足だった。**対応:** DecisionsBrowser と URL `q` を共有、CLI exit 1 統一、404/400 分岐、TC-0010 UI 除外・3 秒タイムアウト・MCP 構造化エラー。**狙い:** 利用者向け契約が実装と一致する。 |
| ガバナンス整備（フェーズ4） | **状況:** 旧 REQ/SPEC が残存し、地層・性能 TC が不足していた。**対応:** REQ/SPEC 0011/0016 を superseded、TC-0039〜0041 新設、TC-0024 を system へ再分類、validate-docs と自動テストを更新。**狙い:** 品質ゲートが現行契約を検証する。 |

### Outcome

TraceWeave の文書・実装・テストが同一契約を語れる状態になった。ダッシュボードのカタログ検索が URL と同期し、CLI/MCP/Web API のエラー表現が仕様どおりになる。MCP と adopt のシステム境界が正本に明記され、品質ゲート（lint 142 docs、テスト 94 件）が通る。

### Deferred

- SPEC-0001/0002/0003 のフィールド名（`body` vs `content` 等）の文書統一
- DSN-0003 の HTML インライン注入 vs `data.json` フォールバックの設計判断
- 性能 NFR の REQ 層起票（REQ-0029 以外）

### Improvements

N/A

### Verification

```bash
npm --prefix src run lint   # PASS (142 docs)
npm --prefix src test       # PASS (94 tests)
```

### Notes

走査一覧の優先度 1→4 を単一作業単位で完遂。残存軽微乖離は Deferred に委譲。
