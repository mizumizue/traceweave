---
schema_version: 1
id: AL-20260914-009
kind: agent_log
title: agent-logs を .cursorignore に復帰
task_type: chore
status: done
entrypoint: path-d
trigger: ユーザー指示 — commit
related: [AL-20260914-008]
created: "2026-09-14"
updated: "2026-09-14"
---
## Content

### Objective

**課題:** AL-008 で一時的に `.cursorignore` から `agent-logs/` を外していたが、Agent のコンテキスト汚染を防ぐ書き込み専用運用に戻す必要があった。

**対応:** `.cursorignore` で `agent-logs/` を再度除外し、ルール側の「コンテキスト取得禁止」ポリシーと整合させる。

### Completion Criteria

- [x] `.cursorignore` に `agent-logs/` を復帰
- [x] lint 成功

### Changes

ファイル一覧・差分:

```bash
git show "$(git log --grep 'AL-20260914-009' -1 --format=%H)" --stat
git switch --detach "$(git log --grep 'AL-20260914-009' -1 --format=%H)" && git switch -
```

| Area | Summary |
|------|---------|
| .cursorignore | **状況:** `agent-logs/` がコメントアウトされ、Agent コンテキストに証跡が混入しうる状態だった。**対応:** `agent-logs/` を再び ignore 対象に戻した。**狙い:** 書き込み専用の証跡運用を機械的に担保する。 |

### Outcome

Agent の通常コンテキストから `agent-logs/` が再び除外され、証跡は起票・コミットのみで残り、探索時のノイズが減る。

### Verification

```
npm --prefix src run lint  → exit 0
```

### Notes

人間が IDE でログを閲覧する必要があるときは `.cursorignore` を一時的に外すか、ファイルを直接開く。
