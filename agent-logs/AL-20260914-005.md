---
schema_version: 1
id: AL-20260914-005
kind: agent_log
title: 実行ステータス観測の UI/レポート表示更新
task_type: improvement
status: done
entrypoint: path-d
trigger: ユーザー指示（変更の意味単位分割・ログ・コミット）
related: [REQ-0004, SPEC-0005]
commit: "fadc099b9fc50d1d0bea1627b2748c3c0521963d"
created: "2026-09-14"
updated: "2026-09-14"
---
## Content

### Objective

**課題:** AL-004 でコアスコアリングは実行合格ベースになったが、コンソール出力と Web ダッシュボードは旧表記のままで、ユーザーが「文書件数」と「実行合格件数」を区別できなかった。skipped 状態の TC も UI で判別しづらかった。

**対応:** ConsoleReporter と Web コンポーネント（サマリー、メトリクスグリッド、マトリクス、詳細モーダル、テストランナー）を実行ステータス観測に対応させ、passed / pending / failed / skipped の内訳を表示する。

### Completion Criteria

- [x] ConsoleReporter が実行合格件数とラベルを表示
- [x] Web サマリー・メトリクスグリッドが実行合格ベース表記と件数内訳を表示
- [x] MatrixView / NodeDetailModal / InteractiveTestRunner が skipped 状態を正しく表示
- [x] build.test.ts のペイロード契約を更新
- [x] lint / test 成功

### Changes

ファイル単位の一覧: `commit: fadc099b9fc50d1d0bea1627b2748c3c0521963d`

```bash
git show "$(git log --grep 'AL-20260914-005' -1 --format=%H)" --stat
git switch --detach "$(git log --grep 'AL-20260914-005' -1 --format=%H)" && git switch -
```

| Area | Summary |
|------|---------|
| コンソールレポート | **状況:** ConsoleReporter が旧ラベルで件数を表示し、実行合格ベースのスコアであることが伝わらなかった。**対応:** 実行合格 / 未実行 / 失敗件数とラベルを更新。**狙い:** CLI 利用者がスコアの根拠（実行合格件数）を即座に把握できる。 |
| Web サマリー | **状況:** ダッシュボードの充足度表示が文書件数ベースの印象を与え、エクスポート Markdown にも内訳がなかった。**対応:** App.tsx / QualityMetricsGrid.tsx で実行合格ベース表記と TC 件数内訳を表示。エクスポートにも反映。**狙い:** Web 利用者が「何が通って何が未実行か」を一覧できる。 |
| TC 状態表示 | **状況:** MatrixView / NodeDetailModal / InteractiveTestRunner で skipped や pending の区別が不十分で、evidence が文書由来のまま残っていた。**対応:** skipped のアイコン・配色を追加し、evidence はレポート由来のみ表示。テストランナーで pending / failed / skipped を色分け。**狙い:** 各 TC ノードの実行状態を UI 上で一目で判別できる。 |
| テスト | **状況:** build.test.ts のペイロード契約が旧サマリーフィールドを前提としていた。**対応:** passed / pending / failed サマリーフィールドの契約を更新。**狙い:** Web ビルド成果物のスキーマ変更をテストで固定する。 |

### Outcome

コンソール出力と Web ダッシュボードで、充足度スコアが実行合格ベースであることが明示され、passed/pending/failed/skipped の件数内訳を確認できる。各 TC ノードの実行状態も UI 上で区別できる。

### Improvements

N/A

### Verification

```
npm --prefix src run lint  → exit 0
npm --prefix src test      → 90 tests pass, 0 fail
```

### Notes

前提: AL-20260914-003（ADR-0006 分離）、AL-20260914-004（コアスコアリング）。
