---
schema_version: 3
id: TC-0008
kind: test_case
title: テストケース詳細および実測結果・ステータスのパース・構造化保持テスト
status: accepted
created: "2026-09-12"
updated: "2026-09-12"
scope: local
verifies:
  - REQ-0006
  - REQ-0007
  - SPEC-0006
depends_on: []
tags: [unit, parser, actual-result]
links: []
test_level: unit
test_method: unit_mock
criticality: high
execution_status: passed
---
## Content

### Objective
`DocParser` が `test_case` ドキュメントをパースした際、`execution_status` および `### Actual Results` セクションを含む全セクションが欠損なく抽出され、`DocNode` オブジェクトに保持されることを検証する。

### Preconditions
- 実行ステータス `passed` および `### Actual Results` を含むテストケース Markdown が存在すること。

### Steps
1. `DocParser.parseFile()` にテストケース Markdown ファイルを渡す。
2. 返却された `DocNode` の `execution_status`、`sections`、`actual_result` をアサートする。

### Expected Results
- `node.execution_status` が `'passed'` であること。
- `node.sections['Actual Results']` に実測テキストが格納されていること。
- `node.sections['Objective']` および `node.sections['Expected Results']` が正常に取得できること。

### Actual Results
- パース処理が 1ms 未満で完了し、`execution_status: "passed"`、`actual_result` に指定された検証ログおよび合否結果が正確に抽出された。テストスイート `tests/infrastructure/storage.test.ts` で通過を確認。

### Evidence
- `tests/infrastructure/storage.test.ts` 内の `testCaseParsing` アサーション全件グリーン。
